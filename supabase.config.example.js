/**
 * Shared leaderboard (Supabase)
 *
 * 1. Copy this file to `supabase.config.js` (same folder as index.html).
 * 2. Fill in `url` and `key` from Supabase → Project Settings → API.
 * 3. In Supabase SQL editor, run `leaderboard-schema.sql`.
 *
 * `supabase.config.js` is gitignored so keys are not committed.
 */
window.LEADERBOARD_SUPABASE = {
    url: '',
    key: '',
};
