/**
 * Shared leaderboard — fill these two values from Supabase (Project Settings → API).
 * Safe to commit while empty. If you paste real keys, do not push to a public repo.
 *
 * 1) supabase.com → New project → SQL Editor → paste all of leaderboard-schema.sql → Run
 * 2) Project Settings → API → copy "Project URL" and "anon" "public" key into below
 */
window.LEADERBOARD_SUPABASE = {
    url: '',
    key: '',
};
